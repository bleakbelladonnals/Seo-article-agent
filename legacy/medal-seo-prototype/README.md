# Archived medal SEO prototype

This directory preserves the repository's original Python/CrewAI medal-content prototype. It is historical technical evidence and is not connected to the LumaFlow portfolio application.

Its original dependencies, environment template, tests, WordPress path, and API surface remain intact for traceability. See `CLAUDE.md` and `CHANGELOG.md` for the archived implementation notes. No part of this prototype is required to build or run `frontend/`.
